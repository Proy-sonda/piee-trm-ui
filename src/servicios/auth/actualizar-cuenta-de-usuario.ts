import { ActualizarCuentaRequest } from '@/modelos/actualizar-cuenta-request';

/** **NO USAR DIRECTAMENTE, USAR EL METODO ACTUALIZAR CUENTA DEL `AuthContext`** */
export const actualizarCuentaDeUsuario = (request: ActualizarCuentaRequest) => {
  // TODO: Implementar
  return new Promise<void>((r) => setTimeout(r, 1500));
};
